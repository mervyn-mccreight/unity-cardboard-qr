namespace Assets.Scripts
{
    /// <summary>
    /// Enum mapping the possible QR code types. Ordinal corresponds to JSON type int.
    /// </summary>
    public enum DataType
    {
        Coin,
        Question,
        Particle
    }
}